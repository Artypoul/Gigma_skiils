# Asset paint proof

A critical asset passes only when it is mapped, loaded, visible through its parent chain and has non-empty screenshot crop pixels. Empty canvas or hidden fallback is a failure unless documented as decorative.
