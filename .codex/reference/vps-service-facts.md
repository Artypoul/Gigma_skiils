# Карта фактов сервиса «Твой ВПС»

Опора для роли **консультант для пользователей «Твой ВПС»**: что это за сервис, как человек подключается, какие бывают состояния и **как передать проблему Артёму**. Не выдумывать — если факта тут нет, сверяться с кодом кабинета (`vps_artpol`) или эскалировать.

## Что это

«Твой ВПС» — VPN-сервис на панели **PasarGuard**. У пользователя есть **личный кабинет** (вход по email + одноразовый код). Главное, что ему нужно для подключения — **ссылка-подписка** (`subscription_url`): её копируют из кабинета и импортируют в приложение-клиент (HAPP и аналоги). Одна ссылка = все доступные серверы/конфиги.

## Тарифы и состояния

- **Тарифы:** `free` (бесплатный) и `pro` (платный). Тариф виден в поле `note` у пользователя PasarGuard.
- **Статусы подписки** (PasarGuard): `active`, `disabled`, `limited`, `expired`, `on_hold`.

| Что видит пользователь | Статус | Что значит |
|---|---|---|
| Новый бесплатный | `active` + `note=free` | работает на бесплатном тарифе |
| Ждёт оплаты | `disabled` («Ожидая оплату») | заказ создан, оплата не прошла |
| Оплачен | `active` + `note=pro` | платный тариф активен |
| Закончился | `expired` | срок вышел — нужно продлить |
| Лимит исчерпан | `limited` | упёрся в лимит трафика тарифа |

Канон «бесплатный»: `нет PG-юзера` ИЛИ `status != active` ИЛИ `note == free`.

## Где что в кабинете (роуты `vps_artpol`)

| Раздел | Зачем пользователю |
|---|---|
| `/home` (главная) | VPN-статус + **ссылка-подписка** для подключения |
| `/select-plan`, `/plan` | выбор и просмотр тарифа |
| `/buy`, `/payments` | оплата и история платежей |
| `/usage` | статистика трафика |
| `/support` | форма обращения (тема «Поддержка» / «Возврат») → письмо в сервис |
| `/profile` | профиль, выход |

> Точный публичный адрес кабинета подставляется на проде из окружения — используйте ту ссылку на кабинет, которую выдаёт сервис.

## Известные нюансы инфраструктуры (для агента — когда эскалировать)

Это **сторона сервиса**, пользователь руками не чинит. Могут меняться — при сомнении эскалировать, не утверждать как вечную истину:

- Сейчас сервис работает через ноду в Германии (DE). Один регион.
- На части конфигов маскировка/нестандартный порт может не подниматься — если **конкретный** конфиг из подписки не подключается, пробовать **другой конфиг из той же подписки**.
- На некоторых сетях скорость отдачи режется (фрагментация трафика) — если стабильно медленно на всех конфигах, это вопрос к сервису → эскалация.

## Связь и эскалация (передать проблему Артёму)

**Основной канал — Telegram:** [@artypoul](https://t.me/artypoul).

Запасные (уже встроены в продукт):
- Форма в кабинете `/support` (тема «Поддержка» или «Возврат») → уходит письмом в сервис.
- Email сервиса: `2141349@mail.ru` (указан и на лендинге).

**Что собрать перед эскалацией** (чтобы Артём решил быстро, без переписки):
1. Устройство и ОС (например: iPhone iOS 18 / Android 14 / Windows 11).
2. Приложение и версия (например: HAPP 2.x).
3. Email/логин в кабинете + тариф и статус (free/pro, active/expired/…).
4. Что именно не работает и с какого момента.
5. Что уже пробовали (из vps-troubleshoot).
6. Текст ошибки или скриншот.

⚠ **Никогда не просить пароль.** Вход в кабинет — по одноразовому коду. Ссылка-подписка приватна — не публиковать в открытых чатах.
